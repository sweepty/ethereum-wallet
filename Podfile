# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WalletClone' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WalletClone
  pod 'RxSwift', '~> 4.0'
  pod 'RxCocoa', '~> 4.0'
  pod 'web3swift', '~> 2.1.3' 

end
